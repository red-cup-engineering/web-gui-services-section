import assert from"node:assert/strict";import test from"node:test";import{WEB_GUI_CLIENT,WEB_GUI_MATERIAL_CSS,normalizeWebGuiModel,renderWebGuiDocument}from"../src/index.mjs";
const model={revision:"ni:///sha-256;witness",title:"Ceramic Jianghu",actor:{id:"a",name:"Sweeper"},locus:{id:"court",name:"Broken Court"},entities:[{id:"court",name:"Broken Court",charge:"stable",position:{x:0,y:0,z:1},detail:"Both witnesses retained."},{id:"gate",name:"Zeolite Gate",charge:"potential"}],paths:[{from:"court",to:"gate",charge:"active"},{from:"court",to:"invented"}],actions:[{id:"look",label:"Attend"}],journal:[{id:"j",tone:"contradiction",summary:"The lacquer holds both accounts."}],faculties:[{id:"p",name:"Perception",level:61,voice:"The crack is information.",active:true}],witness:{status:"live",receipt:"urn:witness:1"}};
test("normalization preserves structure and rejects invented edges",()=>{const v=normalizeWebGuiModel(model);assert.equal(v.entities[0].charge,"stable");assert.deepEqual(v.paths.map(x=>x.to),["gate"]);assert.ok(Object.isFrozen(v))});
test("document is executable, accessible, dependency-free, and escaped",()=>{const h=renderWebGuiDocument({...model,title:"<img src=x>"},{source:"/api/view",actionEndpoint:"/api/input"});assert.match(h,/&lt;img src=x&gt;/);assert.doesNotMatch(h,/<img src=x>/);assert.match(h,/aria-label="Available actions"/);assert.doesNotMatch(h,/https?:\/\//);assert.doesNotThrow(()=>new Function(WEB_GUI_CLIENT))});
test("contradiction uses a non-color channel and motion is bounded",()=>{assert.match(WEB_GUI_MATERIAL_CSS,/repeating-linear-gradient/);assert.match(WEB_GUI_MATERIAL_CSS,/prefers-reduced-motion/);assert.match(WEB_GUI_MATERIAL_CSS,/@media\(max-width:540px\)/)});
